There are comments at the top of every GML file explaining when its called and various other things.
Any instance of "ENUM_CHARACTERID" gets replaced with the characters ID.
Creating a file named "Collision_(INSERT OBJECT NAME HERE)" will prepend whatever code you put inside to the collision event for that object, assuming the player already had an event for said object.
